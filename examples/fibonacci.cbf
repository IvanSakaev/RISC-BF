jmp <begin>

out 0x21

begin:
bl1:
bl2:
bl3:

mov R1 0x9
mov R2 0x0
mov R3 0x1

loop:
    mov  R4 0
    add  R4 R2
    add  R4 R3

    mov  R2 R3
    mov  R3 R4

    subm R1 1
    jnz  R1 <loop>

out R2

